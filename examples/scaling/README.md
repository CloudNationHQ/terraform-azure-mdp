# Scaling

This example illustrates manual resource predictions, pre-provisioning agents during business hours to reduce pipeline wait times.

> **Note:** This example requires an Azure DevOps organization. Set `ado_organization_url` to your organization URL (e.g. `https://dev.azure.com/myorg`) before running.
