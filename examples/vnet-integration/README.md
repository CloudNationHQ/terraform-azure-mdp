# Vnet Integration

This example illustrates deploying a managed devops pool with agents running inside a dedicated subnet.

> **Note:** This example requires an Azure DevOps organization. Set `ado_organization_url` to your organization URL (e.g. https://dev.azure.com/\<your-org\>) before running.
